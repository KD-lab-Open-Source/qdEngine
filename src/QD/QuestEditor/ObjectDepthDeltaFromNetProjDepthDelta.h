#pragma once

/// Класс должен был по приращению глубины проекции на сетку
/// вычислить прирощение глубины объекта
/**
	Класс должен был по приращению глубины проекции на сетку
	вычислить прирощение глубины объекта. Но этого так и не случилось.
	Класс так ничего и не делает
 */
class ObjectDepthDeltaFromNetProjDepthDelta
{
	ObjectDepthDeltaFromNetProjDepthDelta(void);
	~ObjectDepthDeltaFromNetProjDepthDelta(void);
public:
	static void calc();
};
